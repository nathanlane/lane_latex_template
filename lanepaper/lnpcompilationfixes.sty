%% Copyright 2025-2026 Nathan Lane
%%
%% This work may be distributed and/or modified under the
%% conditions of the LaTeX Project Public License, either version 1.3c
%% of this license or (at your option) any later version.
%% The latest version of this license is in
%%   https://www.latex-project.org/lppl.txt
%% and version 1.3c or later is part of all distributions of LaTeX
%% version 2008 or later.
%%
%% This work has the LPPL maintenance status `maintained'.
%%
%% The Current Maintainer of this work is Nathan Lane.
%%
%% This work consists of the files in lanepaper/.
%%
% ==============================================================================
% LNPCOMPILATIONFIXES.STY - Minimal Fixes for Common Compilation Warnings
% ==============================================================================
% Simplified version with fewer hooks to avoid conflicts
% ==============================================================================

\ProvidesPackage{lnpcompilationfixes}[2026/08/12 v1.1 Simple Compilation Fixes]
\RequirePackage{etoolbox} % for \robustify (#55)

% ==============================================================================
% OVERFULL HBOX FIXES
% ==============================================================================

% Add hyphenation exceptions for problematic words
\hyphenation{
  % Typography terms that cause overfull boxes
  Pro-duc-tion-Grade
  Type-writer
  Post-Script
  % Add more as needed
}

% ==============================================================================
% BIBLATEX WARNINGS FIX
% ==============================================================================

% Fix for "conflicting options" warning
% This needs to be done BEFORE biblatex is loaded
\@ifpackageloaded{biblatex}{%
  % Too late to fix
}{%
  % Pre-configure biblatex to avoid conflicts
  \PassOptionsToPackage{uniquename=init}{biblatex}%
}

% ==============================================================================
% OVERFULL VBOX FIXES
% ==============================================================================

% Increase flexibility for page breaking
%% FIX: these 9999 values are for STANDALONE use of this module; under the
%% full package the canonical 10000 block in lanepaper.sty loads later
%% and wins. Removing them silently dropped widow/orphan protection and
%% \raggedbottom for the documented standalone-module pattern.
\widowpenalty=9999      % Almost forbid widows (standalone fallback)
\clubpenalty=9999       % Almost forbid orphans (standalone fallback)
\raggedbottom           % Allow variable page heights (standalone fallback)

% More flexible display spacing
\setlength{\floatsep}{12pt plus 4pt minus 4pt}
\setlength{\textfloatsep}{15pt plus 4pt minus 4pt}
\setlength{\intextsep}{12pt plus 4pt minus 4pt}

% ==============================================================================
% TABLE WIDTH FIXES
% ==============================================================================

% Commands for handling wide tables
\newcommand{\fitwide}[1]{%
  \resizebox{\linewidth}{!}{#1}%
}

% ==============================================================================
% GENERAL COMPILATION HELPERS
% ==============================================================================

% Draft mode helper - shows overfull boxes
\newcommand{\showoverfulls}{%
  \overfullrule=5pt % Show black bars for overfull boxes
}

% Final mode helper - hide overfull boxes
\newcommand{\hideoverfulls}{%
  \overfullrule=0pt % Hide black bars
}


% ==============================================================================
% ROBUSTNESS (#55)
% ==============================================================================
% Every public macro defined above is made robust for moving arguments
% (section titles, captions, marks, the .aux round-trip) via etoolbox
% \robustify, which applies e-TeX \protected. Definitions stay \newcommand
% so a name collision with another package still errors at load time.
% \ifdefmacro skips names that resolve to registers in this load order.
\ifdefmacro{\fitwide}{\robustify{\fitwide}}{}
\ifdefmacro{\showoverfulls}{\robustify{\showoverfulls}}{}
\ifdefmacro{\hideoverfulls}{\robustify{\hideoverfulls}}{}

\endinput
