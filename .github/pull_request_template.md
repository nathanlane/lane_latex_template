## Summary

<!-- What changed and why -->

## Test plan

- [ ] `make build` passes (LaTeX compilation)
- [ ] `python3 -m pytest -q` passes (27 regression tests)
- [ ] `bash tests/run-tests.sh` passes (fixture and compatibility suite)
