## Summary

<!-- What changed and why -->

## Test plan

- [ ] `make build` passes (LaTeX compilation)
- [ ] `make lint` passes (ChkTeX over the demo sources)
- [ ] `make test` passes (pytest, then the shell fixture and compatibility suite)
